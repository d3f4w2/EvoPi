# V2 增量 · <功能名>（属于哪个 V1 模块 / 或新模块）

> V2 是**增量**，不是重来。这份文档只讲"这次变了什么"，并强制回答"从 V1 什么 → 变成 V2 什么"。
> 复制本模板写新功能的增量设计。骨架对齐 V1 的模块设计文档，但专为"增量"设计。

## 1. V1 现状（从哪儿接着改）

引用 V1 对应文档，一句话说清现在是什么样、到哪为止。**先查证 Pi 内核有没有已经做了这件事**（避免重复造轮子）。

## 2. 这次增量（改什么）

只写新增/改动的部分。标明是**加法**（向后兼容）还是**改造**（牵动下游 → 链接对应 ADR）。

## 3. 实现前后的区别（最关键）

| 维度 | 实现前（V1 / Pi 原生） | 实现后（V2） |
| --- | --- | --- |
| … | … | … |

## 4. 怎么做 + 落点

动哪个文件、加什么函数、挂哪个钩子、**不碰什么**。

## 5. 怎么验收（Done = 全绿）

可度量的判据：补哪些测试、（可选）真实验证怎么量化。

## 6. 再往后（V3 留口）

这块以后还能怎么进化。
