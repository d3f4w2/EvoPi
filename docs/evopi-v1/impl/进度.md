# EvoPi 实现进度表

> 本文件是 EvoPi **实现阶段**的单一事实源（区别于 `../进度.md` 那张「讨论/方案进度表」）。
> 每次开新窗口/压缩后**先读这里**：确认「当前在实现哪个模块、做到哪一步、验收过没」。任务边界 = 这张表，**表里没有的 V1 不做**。
> 规则见 [README.md](README.md)（实现契约模板 + 控-token 约定 + 拆文件规范）。

最近更新：2026-07-01

---

## 冻结的全局约定（实现期不得漂移）

这些一旦开工就**冻结**，改动需显式记一笔理由到本表「变更日志」：

1. **事件词表冻结**：各模块 `*.*` 事件的名字与字段以对应方案文档的 schema 为准（如 Cost 见 [02-上下文成本机制.md](../02-上下文成本机制.md) 决策 4）。下游 SQLite / V2 自动化都依赖它不变。**加字段可以（向后兼容），改名/删字段要记变更日志。**
2. **Anchor-only**：只有「资产产生/审批/关键治理决策」写 session anchor，逐次观测只进 JSONL。判据见 [00-整体架构.md](../00-整体架构.md) 第四节。
3. **安全 > 资源**：`tool_call` 是单 handler，模块 4（安全准入）先于模块 5（预算资源）。
4. **共享 policy**：模块 3/4/5 读同一份 `.pi/evopi/policy.json`，不各造一套。
5. **保守默认**：总是拦高危、无 UI fail-safe、评分不猜、自动决策留 V2。

---

## 实现流水线（每模块都走这一条）

```text
① 冻结契约（schema/挂点/验收 —— 直接引方案文档，不重写）
② 拆子文件接线（按模块拆 .ts，index.ts 只注册+路由）
③ 核心逻辑
④ 命令 / UX
⑤ 自测：跑命令 → 看到预期 trace 事件 → 验收判据全绿
⑥ 小步中文提交 → 勾本表
```

每模块 5 个可勾选步骤对应下面的状态列。

---

## 模块实现总览

| 顺序 | 模块 | ①契约 | ②接线 | ③逻辑 | ④命令 | ⑤验收 | 落点文件 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | **脚手架**（进度表/契约模板/防失忆 hook） | ✅ | ✅ | ✅ | ✅ | ✅ | `docs/evopi-v1/impl/`、`.claude/settings.json`、`.claude/hooks/` |
| 2 | Cost 上下文成本 | ✅ | ✅ | ✅ | ✅ | ✅ | `evopi-trace/cost.ts` + `trace.ts`（底座抽出） |
| 3 | 技能记忆 | ✅ | ✅ | ✅ | ✅ | ✅ | `evopi-trace/memory.ts` + `skill.ts` + `policy.ts`（共享，本模块先建） |
| 4 | 执行治理 | ✅ | ✅ | ✅ | ✅ | ✅ | `evopi-trace/job.ts`（复用 policy.ts；Policy Gate 走 index.ts 单 tool_call handler） |
| 5 | 工具运行时 | ✅ | ✅ | ✅ | ✅ | ✅ | `evopi-trace/tools.ts`（预算接在 index.ts 单 tool_call handler 里 policy 之后） |
| 6 | 评测协作 | ✅ | ✅ | ✅ | ✅ | ✅ | `evopi-trace/eval.ts` + `subagent.ts`（spawn 拆出控行数） |

状态：⬜ 未开始 / 🔄 进行中 / ✅ 完成。**🎉 V1 六模块（2-6）全部 ✅ 完成，实现阶段闭环。**

---

## 各模块「实现清单 + 验收」

> 契约（schema/挂点/逻辑）在方案文档里，这里只补**拆哪些文件**和**验收判据**（怎么算做完）。

### 模块 2 — Cost 上下文成本

**契约**：[02-上下文成本机制.md](../02-上下文成本机制.md)（挂点见二节、事件 schema 见决策 4、口径见处理流程）。**冻结**：`cost.request`/`cost.cache`/`cost.pressure` 三事件字段。

**实现清单**：
- [x] 从 `index.ts` 迁出 `/evopi-cost` 与 provider 钩子逻辑到 `cost.ts`，`index.ts` 只 `registerCost(pi, shared)`。（顺带抽 `trace.ts` 底座——cost 依赖它，是模块 2 的②接线步骤）
- [x] `before_provider_request`：记 `cost.request` + 查 `getContextUsage().percent` 跨阈值告警。
- [x] `after_provider_response`：读最近 assistant `.usage` 算命中率，记 `cost.cache`，in-memory 累加（按 entry id 去重，防错误响应重复累加）。
- [x] `cost.pressure`：首次跨 80/90/95% 各记一次（不每轮记；回落重置 floor 允许再报）。
- [x] `/evopi-cost`（摘要）+ `/evopi-cost detail`（原始 usage/payload/路径）。

**验收判据（Done = 全绿）**：
1. 正常对话一轮后 `/evopi-cost` 显示：命中率、本会话累计 token/成本、上下文压力%+档位、请求数。
2. `.pi/evopi/traces/<traceId>.jsonl` 里能 grep 到 `cost.request` 和 `cost.cache`，字段与决策 4 表一致。
3. 上下文压力人为推高（长对话）跨 80% 时，弹一次 notify 且 JSONL 出现一条 `cost.pressure`（同一档位不重复弹）。
4. `cost.*` **不**出现在 session（`grep evopi.trace` 的 session anchor 里查不到 cost.*）——守 Anchor-only。
5. compaction 后 `getContextUsage()` 为 null 时 `/evopi-cost` 不崩（标「估算不可用」）。

**验收证据（2026-07-01，全绿）**：因本环境跑不起完整 Pi+provider，用「真码离线驱动」自测替代 live run：

- **类型检查**：借 `pi/node_modules` 的 tsc 5.9.3，strict 模式对 `trace.ts`/`cost.ts`/`index.ts` 编译 0 错。
- **纯逻辑单测 19/19**：命中率口径（含除零兜底）、`extractUsage` 只认带 usage 的 assistant entry、压力跨档+同档去重+回落重置+null 兜底。
- **集成测 28/28**：用 fake `pi`/`ctx` 加载**真** `registerCost`，驱动 before/after 钩子后**读磁盘 JSONL** 断言：① 3 事件字段与决策 4 逐字段一致（判据 2）② 压力跨 80% 只弹 1 次 notify + 1 条 `cost.pressure`（判据 3）③ session anchor 里无 `cost.*`（判据 4）④ 摘要含命中率/成本/压力/请求数、null usage 标「估算不可用」不崩（判据 1、5）⑤ 错误响应（无新 assistant）不重复累加、不重复记 `cost.cache`。
- 遗留：真 Pi 会话下的端到端 run 待有环境时补一次（当前逻辑与写盘路径已在真码上验证）。

### 模块 3 — 技能记忆 ✅

**契约**：[03-技能记忆机制.md](../03-技能记忆机制.md)。**冻结**：`memory.*`/`skill.*` 事件；memory 三级 scope；trust 四级；`policy.json` 结构（04 决策 4）。

**拆文件**：`memory.ts`（Memory 层）+ `skill.ts`（Skill 层）+ `policy.ts`（模块 3/4/5 共享，本模块先建，4/5 直接复用）。

**实现清单**：
- [x] `policy.ts`：加载 `.pi/evopi/policy.json`（缺失/坏 JSON 退保守默认；与默认**合并**不替换）+ `scanDangerousText`/`matchDangerousCommand`/`matchProtectedPath` 原语（模块 3 静态扫、4/5 运行时扫共用，决策 9）。
- [x] `memory.ts`：三级 scope（project=`.pi/evopi/memory/`、user=`~/.evopi/memory/`、global V1 占位）；结构化条目解析/序列化（兼容旧 MVP 流水账）；`context` 钩子按关键词+tags+时间邻近本地打分注入 Top-K（CustomMessage、display:false、先滤旧再注入）；`session_before_compact` 抢救成 `candidate`（不注入）；`/evopi-memory` add(`--user/--global/--tag`)/review/摘要。
- [x] `skill.ts`：极简 frontmatter 解析（trust 四级）；`resources_discover` **只返回 trusted 的目录**（additive 语义，candidate/untrusted/blocked 不加载）；危险扫描命中→强制 `blocked`（覆盖声明，决策 9）；`tool_call` read 命中 SKILL.md 路径=一次 invoke（统计采集不用，决策 7）；`/evopi-skill` 列表/review/approve/block。
- [x] index.ts：`registerMemory`/`registerSkill`；删旧 `/evopi-memory` MVP；`memory.write`/`skill.approved` 走 anchor。

**验收判据（Done = 全绿）**：
1. `/evopi-memory add <fact>` 写结构化条目（active、带 scope/tags），JSONL 有 `memory.write` 且**写了 session anchor**；`--user` 写到 `~/.evopi/memory/`。
2. 正常一轮里 `context` 钩子按最后一条 user 文本检索，命中的 active 记忆以 `customType:"evopi-memory"`（display:false）注入 messages，JSONL 有 `memory.retrieve`（**不** anchor）；重复轮不叠加（先滤旧）；无命中不注入。
3. 压缩前 `session_before_compact` 落一条 `status: candidate` 记忆（不注入），`/evopi-memory review` 列得出。
4. `.pi/evopi/skills/<name>/SKILL.md` 中：trusted 被 `resources_discover` 返回加载；candidate/无 trust 字段/命中危险的都**不**加载；JSONL 有 `skill.filtered`（不 anchor）。
5. `/evopi-skill approve <name>` 把 candidate 改 `trust: trusted`（写 `approved_by`），JSONL 有 `skill.approved` 且**写 anchor**；对命中危险的 skill approve 被**拒绝**（强制 blocked）。
6. `skill.*`/`memory.retrieve` 等逐次事件**不**出现在 session anchor（守 Anchor-only，仅 `memory.write`/`skill.approved` anchor）。

**验收证据（2026-07-01，全绿）**：同模块 2，用「真码离线驱动」自测（本环境无 live Pi+provider）：
- **类型检查**：tsc strict + noUnusedLocals/Parameters 对 6 个 .ts（trace/cost/policy/memory/skill/index）编译 **0 错**。
- **集成测 44/44**：fake `pi`/`ctx` 加载**真** registerMemory/registerSkill/policy，读磁盘 MEMORY.md/SKILL.md/JSONL 断言全部 6 条判据 + policy 合并/坏 JSON 兜底 + 危险扫描 + 检索排序/滤旧/无命中 + trust 过滤（含「声明 trusted 但命中危险→强制 blocked」）+ approve 拒危险 + 统计采集（read 命中 SKILL.md）+ 非 skill tool_call 不误计。
- **JSONL 目测**：8 个事件（memory.write/review/retrieve、skill.invoke/outcome/approved/blocked/filtered）字段与决策 10 一致；**anchored 集合恰为 {memory.write, skill.approved}**，Anchor-only 守住。
- **模块 2 回归**：logic 19/19 + 集成 28/28 仍全绿（trace.ts/index.ts 改动无回归）。
- 遗留：真 Pi 会话下 resources_discover/context 端到端 run 待有环境补一次。

### 模块 4 — 执行治理 ✅

**契约**：[04-执行治理机制.md](../04-执行治理机制.md)。**冻结**：`job.*`/`policy.*` 事件；风险三级（low/medium/high）；policy.json 结构（决策 4）。

**拆文件**：`job.ts`（Job 信封 + Policy Gate + checkpoint/rewind + 证据验收）；复用模块 3 建的 `policy.ts`。**关键架构**：Policy Gate 不自注册 tool_call，导出 `evaluateToolCall` 交给 index.ts 的**单一 tool_call handler**（安全>资源，模块 4 先于 5，避免多 handler 顺序陷阱）。

**实现清单**：
- [x] `job.ts` 迁出 Job 信封 MVP + 补字段（checkpoints/lastEntryId/highRiskBlocked/tests）。
- [x] `classifyRisk`：工具类型 + 内容扫共享 policy → low（只读工具/只读 bash）/medium（write/edit/写 bash）/high（bash 命中危险黑名单、write/edit 命中受保护路径）。
- [x] Policy Gate（`evaluateToolCall`）：low 放行不记；medium 打 checkpoint + 放行 + `policy.check`；high 打 checkpoint → `ctx.ui.confirm` → 批准放行(`policy.approved`)/拒绝 block(`policy.denied`)/**无 UI 一律 block**(`policy.blocked`, fail-safe)。
- [x] 「记住选择」收紧：只对**命令指纹**生效；**危险 bash 命令不允许记住**（每次问），受保护路径写可记住。
- [x] checkpoint：medium/high 写前自动 `setLabel` + 存 job；`/evopi-job checkpoint` 手动。rewind：`/evopi-job rewind [n|entryId]` 调 `ctx.navigateTree`（V1 只手动）。
- [x] 证据验收：`turn_end` 记 entryId 供 rewind；`tool_result` 识别测试命令(policy.testCommandPatterns)+isError 记 testsPassed；`/evopi-job` 证据面板；切 `passed` 时有 error/高危未解决/测试没过则警告。
- [x] index.ts：单 tool_call handler 先跑 `job.evaluateToolCall`（block 立即返回）再观测；`registerJob`；删旧 `/evopi-job` MVP。

**验收判据（Done = 全绿）**：
1. `rm -rf`/force-push/`npm publish` 等高危 bash → 有 UI 弹 `confirm`，拒绝则该 tool_call 被 block（错误结果回模型），JSONL 有 `policy.denied`（anchor）；批准则放行 + `policy.approved`（anchor）。
2. 无 UI（hasUI=false）时高危一律 block（fail-safe），JSONL 有 `policy.blocked`（anchor，mode=no-ui）。
3. 只读工具（read/grep/git status）放行且**不**记 `policy.check`；写操作（write/edit/写 bash）放行 + 记 `policy.check`(medium) + 写前打 checkpoint。
4. `/evopi-job checkpoint` 打标签（`setLabel`）并存进 job；`/evopi-job rewind` 调 `navigateTree` 回退，JSONL 有 `job.checkpoint`/`job.rewind`（anchor）。
5. `/evopi-job` 证据面板显示 toolErrors/highRiskBlocked/checkpoints/tests；切 `passed` 有未解决 error/高危/测试失败时弹警告。
6. `policy.check` 只进 JSONL（不 anchor）且只记 medium/high；`policy.approved`/`denied`/`blocked` + `job.start`/终态/checkpoint/rewind 写 session anchor（守决策 8）。

**验收证据（2026-07-01，全绿）**：同前，「真码离线驱动」自测（本环境无 live Pi+provider）：
- **类型检查**：tsc strict + noUnusedLocals/Parameters 对 7 个 .ts（+job.ts）编译 **0 错**。
- **集成测 41/41**：fake `pi`/`ctx`（可控 hasUI/confirm 答案/setLabel/navigateTree 捕获）加载**真** registerJob，断言全部 6 判据 + classifyRisk 11 例（含 rm -rf/force-push/写 .env/.git 判 high）+ 「记住」收紧（危险 bash 不记住、受保护路径写可记住）+ 证据面板 + rewind 导航。
- **JSONL 目测**：8 事件（policy.check/approved/denied/blocked、job.start/checkpoint/rewind/status）字段与决策 8 一致；**anchored 集合恰为 {policy.approved/denied/blocked, job.start/checkpoint/status/rewind}，policy.check 不在内**，Anchor-only 守住。
- **模块 2/3 回归**：logic 19/19 + m2 集成 28/28 + m3 集成 44/44 全绿（单 tool_call handler 重构无回归）。
- 遗留：真 Pi 会话下 tool_call block/confirm/navigateTree 端到端 run 待有环境补一次。

### 模块 5 — 工具运行时 ✅

**契约**：[05-工具运行时机制.md](../05-工具运行时机制.md)。**冻结**：`tool.result` 扩展字段 errorClass/latencyMs、`tool.budget`；7 类错误（timeout/auth/schema/permission/rate_limit/tool_bug/unknown）。**与模块 4 共用 tool_call 单 handler（安全>资源，模块 5 后于 4）。**

**拆文件**：`tools.ts`（错误分类 + 延迟 + 预算 + `/evopi-tools`）；toolStats/tool_result 观测从 index.ts 迁入。

**实现清单**：
- [x] `tools.ts` 错误 7 类启发式分类（内置关键词规则表，顺序敏感：rate_limit 先于 auth；含 bash 原生 `timeout:N`）。
- [x] 延迟计时：tool_call 记 startTime（按 toolCallId），tool_result 算差；toolStats 加 `errorsByClass` + `latency{count,totalMs,maxMs}`（avg=total/count）。
- [x] 预算：分工具 + 全局，per-turn（turn_start 重置，防单轮死循环）+ per-job（会话累计）；**硬限只卡调用次数**（per-turn 50/per-job 200/全局 300），延迟/错误只软限告警（80%）。
- [x] `evaluateBudget`/`onToolCall`/`onToolResult`/`resetTurn` 接口；`tool.budget` 事件（soft 只 JSONL / hard block 写 anchor）；`tool.result` 加 errorClass/latencyMs。
- [x] `/evopi-tools` 展示错误分类分布 + 延迟 avg/max + 预算用量。
- [x] index.ts 单 tool_call handler：policy 放行后接 `tools.evaluateBudget`（安全>资源）；tool_result 调 `tools.onToolResult`；turn_start 调 `resetTurn`。
- [ ] ~~工具级超时（决策 3）~~ → **改 V2**（Pi 扩展 tool_call 不暴露单工具 AbortController，见变更日志；bash 原生超时兜底）。

**验收判据（Done = 全绿）**：
1. 工具失败时 `tool.result` 带 `errorClass`（timeout/auth/permission/… 7 类之一），成功时无 errorClass；`toolStats.errorsByClass` 累计。
2. 每次 tool_result 的 `tool.result` 带 `latencyMs`（tool_call→tool_result 时间差，按 toolCallId 配对）。
3. 单工具本轮调用达 80% 阈值 → 记一条 `tool.budget`(soft，只 JSONL)；达硬限 → `evaluateBudget` 返回 block（错误结果回模型）+ `tool.budget`(hard，anchor)。
4. **安全 > 资源**：既高危又超预算的工具，block 原因取 **policy**（安全）而非预算；纯超预算取预算原因。
5. `turn_start` 重置 per-turn 预算（新一轮工具计数归零）。
6. `/evopi-tools` 显示「bash: N 次/M 错(timeout:x)/均 Yms/最慢 Zms [本轮 a/50]」；`tool.result` 只进 JSONL、`tool.budget` 硬限 anchor（守决策 8）。

**验收证据（2026-07-01，全绿）**：同前「真码离线驱动」自测：
- **类型检查**：tsc strict + noUnusedLocals/Parameters 对 8 个 .ts（+tools.ts）编译 **0 错**。
- **集成测 67/67**（tools.ts）：classifyError 11 例（7 类 + 顺序 + bash timeout:N + unknown 兜底）+ 延迟配对（成功无 errorClass）+ 错误入 stats/tool.result + 预算软限(80% used=40)/硬限(block+anchor)/resetTurn + renderStats。
- **接线测 6/6**（m5wire，加载**完整扩展**驱动真实单 tool_call handler，按 Pi「全执行、最后非 undefined 生效」语义）：低危放行 + 高危拒绝 block(policy 原因) + 超预算 block(预算原因) + **既高危又超预算取 policy 原因**（安全>资源确证）。
- **JSONL 目测**：`tool.result` 带 errorClass/latencyMs、`tool.budget` soft{used,limit,percent}/hard{used,limit}；**anchored 仅 tool.budget(hard)，tool.result 与 soft 不 anchor**，守决策 8。
- **模块 2/3/4 回归**：19/19 + 28/28 + 44/44 + 41/41 全绿（共享 tool_call/tool_result 重构无回归）。
- 遗留：真 Pi 会话下端到端 run 待有环境补；工具级超时 V2。

### 模块 6 — 评测协作 ✅（V1 最后一个）

**契约**：[06-评测协作机制.md](../06-评测协作机制.md)。**冻结**：`eval.*` 事件（score/run/replay/gate/candidate）；agent card 字段（name/description/tools/model + EvoPi 加 maxTurns）；golden task YAML 结构（id/prompt/checks/scoring）。

**拆文件**：`eval.ts`（确定性评分引擎 + golden task + 棘轮门禁 + `/evopi-eval`）+ `subagent.ts`（spawn pi 子进程 + agent card 解析，从 eval.ts 拆出控行数 ~500）。

**实现清单**：
- [x] golden task YAML 解析（极简自包含解析器：扁平 scalar + `checks:` 内联 `- {k:v}` 列表，含 no_forbidden 的 `[...]` 数组）。
- [x] 确定性检查（决策 2）：command（`<cmd>; echo EXIT:$?` 取退出码，决策 6）/file_changed（git status）/output_contains/no_forbidden（复用 policy 思路查禁区路径）。
- [x] 评分（决策 5）：默认 all_checks_pass 二值；weighted 平均；绑 evalId/taskId/traceId/datasetId 可追溯。
- [x] 子代理 spawn（决策 3）：`subagent.ts` 真 spawn `pi --mode json -p --no-session [--model][--tools]`，监听 message_end 计 turn、超 maxTurns kill（决策 4）；agent card frontmatter 解析。
- [x] 棘轮半自动（决策 7）：`replay <task>` 跑全量对比 baseline（历史最佳）展示不裁决→`gate <task> enable|reject` 人拍板（写 anchor，enable 即 skill.accepted 闭环点）；`candidate <task>` 从失败 trace 生成骨架（写 anchor，危险扫描复用 policy）。
- [x] index.ts：`registerEval`；删旧 `/evopi-eval` MVP（record/list）。

**验收判据（Done = 全绿）**：
1. `run <task>` 跑 golden task 确定性 check 打分，PASS/FAIL + 各 check 结果；JSONL 有 `eval.run`（不 anchor）。
2. command check 用 `echo EXIT:$?` 可靠取退出码（`true`→0、`exit 3`→3 via error.status 兜底、真实命令 npm 等自然退出码）。
3. `replay <task>` 展示 candidate vs baseline（不降即可的裁决提示，但不自动 enable）；JSONL 有 `eval.replay`（不 anchor）。
4. `gate <task> enable|reject` 人拍板裁决，写 session anchor（`eval.gate`）；enable 即资产被接受的闭环点。
5. `candidate <task>` 生成 golden task 骨架 + 写 session anchor（`eval.candidate`）；危险扫描复用 policy。
6. 子代理真 spawn（agent card 的 model/tools/maxTurns 生效），超 maxTurns kill；`eval.score/run/replay` 只 JSONL、`eval.gate/candidate` anchor（守决策 8）。

**验收证据（2026-07-01，全绿）**：同前「真码离线驱动」自测：
- **类型检查**：tsc strict + noUnusedLocals/Parameters 对 **10 个 .ts**（+eval.ts/subagent.ts）编译 **0 错**。
- **集成测 42/42**（eval.ts）：golden task YAML 解析（4 check 类型 + 缺 id 兜底）+ 确定性检查（注入 exec/fileChanged 的 pass/fail + all_pass 0/1 + weighted 0.75）+ **真命令退出码**（`true`/`exit 3`/echo 经 sh 跑 echo EXIT:$?）+ agent card 解析 + run/replay/gate/candidate 命令 + baseline 追踪 + anchor 判据（只 gate/candidate anchor）。
- **子代理测 7/7**（m6spawn，spawn **stub 子进程**发 canned pi JSON 事件流）：5 turns 计数 + output 收集 + maxTurns=2 kill + 不存在命令优雅报错。
- **JSONL 目测**：`eval.run{taskId,score,passed,checks}`、`eval.gate{decision,baseline,approvedBy}`、`eval.candidate{taskId,sourceTrace,dangerHits}` 字段对齐决策 8；anchored 恰为 {eval.gate, eval.candidate}。
- **模块 2-5 回归**：19/19 + 28/28 + 44/44 + 41/41 + 67/67 + 接线 6/6 全绿（拆分 + 共享无回归）。
- 遗留：真 pi+model 的端到端 spawn 评估 run 待有环境补（spawn/JSON 解析/turn 预算已在真码 stub 上验证）。

---

## 变更日志（改冻结约定必须记这里，含理由）

| 日期 | 改了什么 | 为什么 |
| --- | --- | --- |
| 2026-07-01 | 建实现进度表，进入实现阶段 | 规划闭环，开工 |
| 2026-07-01 | **底座抽出**：新建 `trace.ts`（recorder/事件写入/摘要工具从 index.ts 迁出）；index.ts 只做注册+路由 | 模块 2 的②接线步骤，cost 依赖底座；兑现 README §5「index.ts 只注册、按模块拆子文件」，防 index.ts 继续膨胀 |
| 2026-07-01 | **anchor 语义收敛（非词表改动）**：recorder 写事件默认**只进 JSONL**，`{anchor:true}` 才写 session。index.ts 里 `session.*`/`compact.*`/`session.shutdown`/`job.start`/`job.status`/`memory.write` 显式传 `anchor:true`；其余（turn/tool/agent/message）改为只进 JSONL | 原 MVP 对**每个**事件都 `pi.appendEntry` 写 session＝过度 anchor，违背 00-架构第四节 Anchor-only 表。收敛后与事件总线表一致。**未改任何事件字段/名字**，故非词表冻结破坏；下游不受影响 |
| 2026-07-01 | Cost 累加按 assistant **entry id 去重** | 防「本轮响应无新 assistant（错误/中断）时回溯取到上一轮 assistant，被重复累加 + 重复记 cost.cache」。集成测已覆盖 |
| 2026-07-01 | **policy.ts 提前到模块 3 建**（原表列在模块 4 落点） | 决策 9：模块 3 静态扫 skill 需要共享危险定义，模块 3 是首个消费者，故共享 policy 单一事实源在此建立，模块 4/5 直接复用（避免各造黑名单）。结构=04 决策 4，未改约定 |
| 2026-07-01 | **Skill trust 缺省=candidate；危险扫描命中→强制 blocked（覆盖声明 trust）** | 决策 8/9 落地的安全默认：来源不明按候选不自动可见；声明 trusted 但正文含 rm -rf 等仍强制 blocked、approve 也拒。非新约定，是对决策的保守实现 |
| 2026-07-01 | **Skill 调用信号=read 命中 SKILL.md 路径**（非独立 tool） | 查证 Pi 里 skill 靠模型 read 其 SKILL.md 加载（无独立 skill 工具，见系统提示「Use the read tool to load a skill's file」），故 skill.invoke 用 read 输入命中 skill 文件路径判定 |
| 2026-07-01 | **tool_call 单 handler 落地**：Policy Gate（模块 4）不自注册 tool_call，导出 `evaluateToolCall` 交 index.ts 单一 handler，先安全决策(block 立即返回)再观测 | 兑现冻结约定 3「安全>资源，tool_call 单 handler」+ README §5。模块 5（预算）将接在 policy 之后同一 handler。注：skill.ts 的 tool_call 是纯观测(不 block)故保留独立，单 handler 规则只约束**阻断决策** |
| 2026-07-01 | **风险判定 = 工具类型 + 内容扫 policy**：只读工具/只读 bash→low，write/edit/写 bash→medium，bash 命中危险黑名单 / write·edit 命中受保护路径→high | 决策 3 落地。用 Pi 的 tool_call 事件按 toolName 区分（read/grep/find/ls 只读；edit/write 写；bash 看命令），只读 bash 用正则启发（git status/ls/cat…）归 low 免刷 trace |
| 2026-07-01 | **「记住批准」按危险度分级**：危险 bash 命令**不允许**记住（每次问），受保护路径写可记住（本会话该指纹） | 决策 5 收紧作用域的落地：最危险的每次确认，避免一次误批放行整会话；受保护路径写相对可控，可记住便利 |
| 2026-07-01 | **单 tool_call handler 加入模块 5 预算**：policy 放行后接 `tools.evaluateBudget`，安全>资源；block 原因取先命中者（policy 优先） | 决策 6 落地。确证：既高危又超预算取 policy 原因（m5wire 测）。注：skill.ts 的 tool_call 是纯观测(永不返 block)，与 index.ts 阻断 handler 并存不冲突（Pi「最后非 undefined 生效」，skill 永远 undefined） |
| 2026-07-01 | **模块 5 决策 3「工具级超时(AbortController)」改 V2**（已问用户确认） | 查证：Pi 扩展 tool_call **不暴露单工具 AbortController**，`ctx.signal` 是整轮的、`ctx.abort()` 中断整个 agent，且无「包裹内置工具执行」的 API（registerTool 只能定义新工具，不能 wrap bash/read）。故 V1 不实现；bash 已有原生超时(`timeout:N`)兜底、非 bash 内置工具极少挂起。词表未受影响（errorClass 仍含 timeout，供 bash 超时/未来分类用） |
| 2026-07-01 | **per-job 预算 V1 近似为「随会话累计」**（不真正绑定 job 生命周期） | job 的开始/结束由 `/evopi-job` 手动控制，tools.ts 不强耦合 job；V1 用会话级累计器近似 per-job 上限，够挡「整任务失控」。真正随 job start 重置留 V2（与 job.ts 联动） |
| 2026-07-01 | **模块 6 拆 `eval.ts` + `subagent.ts`** | eval.ts 一体化达 663 行超 README §3 软上限 ~500 → 把子代理 spawn + agent card 解析拆到 subagent.ts（169 行），eval.ts 降到 512。职责分离：「怎么跑评估子进程」vs「评分/棘轮/命令」 |
| 2026-07-01 | **确定性 command check 用 `sh` 跑 `echo EXIT:$?`（跨平台修复）** | 初版误把 `shell:"/bin/sh" as unknown as undefined` 导致 shell 选项被抹掉、Windows 回退 cmd.exe（`$?` 不展开、命令失败静默）。修复：显式 `shell:"sh"`（Git Bash/Linux/macOS 都有）+ 命令自身 `exit N` 时用 `error.status` 兜底真实退出码。集成测 42/42 覆盖 |
| 2026-07-01 | **真 Pi + 真 model 端到端跑通**（补齐统一遗留核心链路） | 用智谱网关 `glm-4-flash` 真跑一轮 `pi --print --mode json`，evopi-trace 扩展真实产出 9 条事件到 JSONL（含 `cost.request` 带真实 provider usage `tokens:8`）。踩通三个源码级坑并固化：① Pi 参考仓需 `npm install`（缺 jiti 扩展加载器/tsx）；② **非交互模式必须 `--approve`**——`--print`/`--mode json` 下 `hasUI=false`→项目信任默认 false→`package-manager.ts` 跳过 `.pi/extensions`；③ 非 TTY 下 prompt 必须走 stdin 管道（否则 `stdin.resume()` 等 EOF 死锁）。可复现脚本+证据见 [impl/e2e-验证/](e2e-验证/) |

---

## 待办指针

**🎉 EvoPi V1 实现阶段闭环——六个模块（2 Cost / 3 技能记忆 / 4 执行治理 / 5 工具运行时 / 6 评测协作）全部 ✅ 完成并自测全绿。**

**代码落点（`.pi/extensions/evopi-trace/`，index.ts 只做注册 + 单一 tool_call 路由）**：

| 文件 | 模块 | 职责 |
| --- | --- | --- |
| `index.ts` | 入口 | 注册各 registerXxx + 单一 tool_call handler（安全>资源）+ trace-core 事件 |
| `trace.ts` | 1 底座 | recorder / 事件写入 / JSONL / anchor 判定 / 摘要工具 |
| `cost.ts` | 2 | cost.request/cache/pressure + /evopi-cost |
| `policy.ts` | 3/4/5 共享 | 危险黑名单 + 受保护路径 + 扫描原语（单一事实源） |
| `memory.ts` | 3 | 三级 scope 记忆 + 关键词检索注入 + 压缩抢救 + /evopi-memory |
| `skill.ts` | 3 | trust 四级 + resources_discover 过滤 + 危险扫描 + /evopi-skill |
| `job.ts` | 4 | Job 信封 + Policy Gate + checkpoint/rewind + 证据验收 + /evopi-job |
| `tools.ts` | 5 | 错误 7 类分类 + 延迟统计 + 预算 + /evopi-tools |
| `eval.ts` + `subagent.ts` | 6 | 确定性评分 + golden task + 子代理 spawn + 棘轮门禁 + /evopi-eval |

**自测总账（真码离线驱动）**：tsc strict + noUnused 10 个 .ts 全绿；集成/逻辑测 **254 断言全过**（m2 28+19、m3 44、m4 41、m5 67+6、m6 42+7）；各模块事件字段对齐方案文档、Anchor-only 判据守住。

**统一遗留 —— 核心链路已闭合（2026-07-01）**：真 Pi + 真 model（智谱 `glm-4-flash`）端到端跑通，扩展真实加载、生命周期事件 + `cost.request`（真实 provider usage）真实落 JSONL。可复现脚本+根因+证据见 [impl/e2e-验证/](e2e-验证/)。
剩余**未由真 Pi 触发**的事件（`tool.call`/`tool.result`/`policy.*`/`tool.budget`/`compact.*`/skill·memory·eval 命令路径）其逻辑与写盘路径已在真码离线驱动自测覆盖（254 断言）；如需真 Pi 触发，扩展验证 prompt 让模型调工具即可（可选深化，非阻塞）。工具级超时（模块 5 决策 3）已明确改 V2。

**下一步（用户定）**：

- ✅ **验证（核心已完成）**：真 pi 跑通、grep JSONL 确认核心事件真实产出。可选深化：扩展 prompt 触发工具/多轮/压缩，补 `tool.*`/`policy.*`/`compact.*` 真 Pi 产出。
- **模块 7 文档产品化 V2**：回填每模块可跑 demo + 真实度量（[07-文档产品化机制.md](../07-文档产品化机制.md) 决策 3 的 demo 模板）。
- **各模块 V2**：Cost 主动缓存 / 技能自进化棘轮 / 执行自动 rewind / MCP+沙箱 / Eval 全自动门禁（都已在 V1 留好接口位）。
