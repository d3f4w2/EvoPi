# EvoPi 讨论进度表

> 本文件是 EvoPi「逐模块讨论」的**单一进度事实源**。每次开新话题前先看这里，确认「当前在讨论哪个模块、哪一项、定了没」。
> 维护规则：每确定一个子议题就更新对应状态；一个模块全部子议题确定后，写「最终方案」文档并把模块状态置为 ✅ 已落文档。

最近更新：2026-06-30

---

## 状态图例

| 标记 | 含义 |
| --- | --- |
| ⬜ 未开始 | 还没讨论 |
| 🔄 讨论中 | 正在逐项讨论，方案未全定 |
| 🟡 方案已定 | 所有子议题已确定，但最终方案文档还没写 |
| ✅ 已落文档 | 已写入 `docs/evopi/<模块>.md` 最终方案（含决策/实现/优点/少量缺点） |
| 👉 | 当前正在讨论的项 |

---

## 模块总览

| 顺序 | 模块 | 状态 | 最终方案文档 | 备注 |
| --- | --- | --- | --- | --- |
| 0 | 核心模型 & 边界（Session/Trace/Eval/Memory 分工） | ✅ 已落文档 | 见各模块文档 + 本表「已锁定的全局决策」 | 已锁定，作为全局约束 |
| 00 | **整体架构**（分层/时序/事件总线/存储/依赖） | ✅ 已落文档 | [00-整体架构.md](00-整体架构.md) | 2026-07-01 补，7 模块决策完成后绘制，含 mermaid 图 |
| 1 | Trace 观测机制（底座） | ✅ 已落文档（MVP 已实现） | [01-Trace观测机制.md](01-Trace观测机制.md) | 扩展已落地 736 行；**仅 JSONL，SQLite 是后续规划** |
| 2 | **上下文成本机制** | ✅ 已落文档 | [02-上下文成本机制.md](02-上下文成本机制.md)（已写最终方案） | 2.0-2.5 全部完成；待实现 |
| 3 | **技能记忆机制** | ✅ 已落文档 | [03-技能记忆机制.md](03-技能记忆机制.md)（已写最终方案） | 3.0-3.7 全部完成；V1=记忆+技能治理，C 自进化推 V2；待实现 |
| 4 | **执行治理机制** | ✅ 已落文档 | [04-执行治理机制.md](04-执行治理机制.md)（已写最终方案） | 4.0-4.7 全部完成；V1=Policy Gate+审批+checkpoint+证据验收；待实现 |
| 5 | **工具运行时机制** | ✅ 已落文档 | [05-工具运行时机制.md](05-工具运行时机制.md)（已写最终方案） | 5.0-5.7 全部完成；V1=错误分类+预算+延迟统计；待实现 |
| 6 | **评测协作机制** | ✅ 已落文档 | [06-评测协作机制.md](06-评测协作机制.md)（已写最终方案） | 6.0-6.7 全部完成；V1=评分引擎+真子代理+棘轮半自动；待实现 |
| 7 | **文档产品化机制** | ✅ 已落文档 | [07-文档产品化机制.md](07-文档产品化机制.md)（已写最终方案） | 7.0-7.5 全部完成；V1=文档收口+面向用户 README；demo/度量待实现后补 |

---

## 当前模块详情：Context Cost Harness（模块 2）

**定位**：主动优化模块，但 V1 只做「缓存观测 + 预算观测」，不裁剪历史、不自动 compact、不做 RAG。

### 子议题清单

| # | 子议题 | 状态 | 结论摘要 |
| --- | --- | --- | --- |
| 2.0 | 缓存这半的整体定位 | 🟡 方案已定 | 选 **A+B**：A=读真实 usage 算缓存命中率写 trace + `/evopi-cost` 展示；B=显示 retention 档位并在合适时**建议**切 long。**C（主动注入 cache_control）砍到 V2**——因为 Pi 已自动用掉 3/4 个 Anthropic 断点，手动注入重复且会挤掉 Pi 的。 |
| 2.1 | `/evopi-cost` 命令形态 | 🟡 方案已定 | **两个命令**：`/evopi-cost`（摘要快照：上下文压力+档位、缓存命中率、本会话累计 token/成本、请求数）；`/evopi-cost detail`（原始 usage / payload / trace path）。**告警**：上下文压力 ≥80/90/95% 时 `ctx.ui.notify` 主动提醒 + 写 `cost.pressure` 到 trace；**不自动 compact、不自动切 retention**。 |
| 2.2 | Cost 写 Trace 的事件结构 | 🟡 方案已定 | **新增 3 个独立事件 `cost.request` / `cost.cache` / `cost.pressure`，不动 `provider.*`**（Trace core 不被 Cost 语义污染，符合「Trace 底座 / Cost 消费者」）。`cost.request`=请求侧（contextEstimate/retention/msg+tool count）；`cost.cache`=响应后读 leafEntry.usage（input/output/cacheRead/cacheWrite/cacheWrite1h + cacheHitRate + cost）；`cost.pressure`=跨阈值时一次（level/percent/tokens）。`cost.*` 只写 JSONL，不写 session anchor（符合 Anchor-only）。 |
| 2.3 | cache hint 白名单策略 | 🟡 基本无悬念 | 因 2.0 选 A+B，此项**收窄为「只调 retention，不注入断点」**；V1 仅观测 + 建议，不主动改 payload |
| 2.4 | 是否需要 Cost 独立 SQLite | 🟡 方案已定 | **V1 只 JSONL + in-memory，不建独立 SQLite**。in-memory 累加供 `/evopi-cost` 即时读；`cost.*` 写 JSONL。不自立门户，将来 Trace 上 SQLite 时 `cost.*` 复用同一 store。理由：与 Trace「JSONL only/SQLite 后置」一致；V1 只需本会话快照+告警，无需跨会话查询（YAGNI）。 |
| 2.5 | Cost 最终方案文档结构 | ✅ 已落文档 | 已写 [02-上下文成本机制.md](02-上下文成本机制.md)，四段式：决策（带理由）/ 实现 / 架构优点 / 少量缺点。 |

### 已查清的 Pi 缓存事实（决策依据，源码已确认）

- Pi 在 Anthropic 上**自动**打 `cache_control`，覆盖 ①每个 system text 块 ②最后一个 tool 定义 ③最后一条 user 消息的最后一个块；Anthropic 总共只允许 4 个断点，Pi 已占 3 个。
- 由 `cacheRetention` 控制：`none` / `short`(5m 默认) / `long`(1h，模型支持时)，env 变量 `PI_CACHE_RETENTION=long`。
- `before_provider_request` 的 `event.payload` 是裸 Anthropic 请求体，handler 返回值能真正改写并发给 provider。
- `after_provider_response` 事件**不含 usage**；真实 token（`cacheRead`/`cacheWrite`/`cacheWrite1h`）在 `ctx.sessionManager.getLeafEntry()` 的 assistant message `.usage` 里。
- `getContextUsage().tokens` 是**估算**，compaction 后到下次响应前为 null。

---

## 当前模块详情：技能记忆机制（模块 3）

**定位**：把核心模型里「Memory/Skill = 从高分/低分 Trace + Eval 沉淀的长期资产」落地。管理项目记忆 + 大规模技能集，不污染 prompt，并支持受控的自我进化。

### 子议题清单

| # | 子议题 | 状态 | 结论摘要 |
| --- | --- | --- | --- |
| 3.0 | Memory 与 Skill 的边界（双层模型） | 🟡 方案已定 | **两层分开，同一模块管**。Memory=数据（事实/偏好/决策，检索后整条注入，低风险）；Skill=可执行逻辑（工作流，Top-K 注入摘要+按需展开全文，需 trust）。两者信任级别/注入策略/演化路径都不同，故分层。**记忆分 project/user/global 三级 scope**：project=`.pi/evopi/memory/`(随仓库)、user=`~/.evopi/memory/`(跨项目)、global=内置/团队共享(V1 留接口)；冲突时 project > user > global（就近原则）；`/evopi-memory add` 默认 project，`--user`/`--global` 写对应层。 |
| 3.1 | 与 Pi 原生 skills 的关系 | 🟡 方案已定 | **①扩展 Pi 的 SKILL.md**：复用 Pi 的加载+注入机制，治理字段（trust/成功失败计数/source-trace-id/审批状态）塞进 Pi frontmatter 额外键（Pi `[key:string]:unknown` 保留但不解释）。理由：同 Cost「能复用 Pi 机制就不另起炉灶」，吃升级红利，一份文件两层都用。**已查证（改了挂点）**：Pi 系统提示只放 skill 的 name+description 列表、全文不进上下文（已是渐进披露）；`context` 钩子**碰不到 skill**（系统提示在它之前固定）。→ Top-K 改挂 `resources_discover`（控制加载哪些 skill 文件）或 `before_agent_start`（改 systemPrompt）。 |
| 3.2 | 记忆存储格式 | 🟡 方案已定 | **轻量元数据 + Markdown 正文**：每条记忆是一个 `##` 小节，带 scope/source/created/tags 字段 + 正文。可 diff/可审查/可检索/可手写。`.pi/evopi/memory/` 保留 INDEX（分类地图+token 成本，借 claude-mem 渐进披露）/MEMORY（结构化非流水账）/SKILLS（技能候选索引）。类型化抽取（profile/event/...，memU 风格）留 V2。 |
| 3.3 | 检索/注入策略（Top-K 路由） | 🟡 方案已定 | **两条路分开**。**Skill**：`resources_discover` 钩子按 trust/审批状态过滤——V1 最大风险是低可信 skill 被模型看见（Pi 已做渐进披露，只注入 name+description），故 V1 只做 trust 过滤，**不做** Top-K 数量裁剪（留 V2，skill 真多了再做）。**Memory**：不会自动进上下文必须检索，走 `context` 钩子改 messages + **关键词/tags/时间邻近本地打分取 Top-K**（mempalace 路线，零 API/零延迟/可解释，与 Cost「零 API 优先」一致）。语义检索留 V2。 |
| 3.4 | 记忆「写入」时机与来源 | 🟡 方案已定 | **保守生效 + 提前积累**。记忆写入三种：①`/evopi-memory add` 显式写直接生效（用户负责）；②`session_before_compact` 压缩前自动**抢救成候选**（标 `status:candidate`，不自动注入，等 `/evopi-memory review` 确认）——解决「失忆」又不污染；③`message_end`/`tool_result` 每轮自动抽取**留 V2**（噪声大、要额外 LLM 判断）。**技能统计**：V1 通过 `tool_call`/`tool_result` **采集但不用**（记 skill 调用次数/成败，写 `skill.*` 事件），为 V2 棘轮自进化铺数据。治理哲学同 Skill：自动产生的都是候选，要过人审。 |
| 3.5 | 技能自进化与信任/审批（棘轮） | 🟡 方案已定 | **V1 做 trust 闸门（V2 棘轮的地基）**。trust 四级写 SKILL.md frontmatter（与 3.1 一致）：`trusted`（人工写/已审批，加载可见）/`candidate`（自动生成或第三方新引入，不加载进上下文但可 review）/`untrusted`（来源不明，不加载）/`blocked`（命中危险动作，永不加载）。来源定初始 trust：人工写→trusted，第三方/自动生成→candidate。命令：`/evopi-skill review`（列 candidate + 来源 trace + 危险扫描结果）/`approve`/`block`。候选元数据带 source/evidence/approved_by/rollback。**危险扫描=黑名单（rm -rf/git reset --hard/force-push/curl\|sh）+ skill 声明调用的工具/路径解析**。⚠️协调点：工具/路径扫描应**复用模块 4/5 的危险动作定义**，不各搞一套；区别=模块3静态准入(审 skill 文本) vs 模块4/5运行时拦截(实际 tool_call)，两道防线。C 层自动生成 skill + 棘轮保留/回滚=V2（依赖模块 6 Eval）。 |
| 3.6 | 写 Trace 的事件结构 | 🟡 方案已定 | 独立 `memory.*` / `skill.*` 事件（同 Cost 2.2，不污染 Trace core）。Memory：`memory.write`/`memory.review`/`memory.retrieve`；Skill：`skill.invoke`/`skill.outcome`/`skill.approved`/`skill.blocked`/`skill.filtered`。**session anchor 判据（细化 Anchor-only，可复用）**：资产的产生/审批=语义锁点→写 session anchor（`skill.approved` + 显式 `memory.write`）；资产的逐次使用/观测=执行细节→只进 JSONL（`skill.invoke`/`outcome`/`memory.retrieve`）。与接力上下文早列的 `evopi.skill.accepted` 应入 session 一致。 |
| 3.7 | 最终方案文档 | ✅ 已落文档 | 已写 [03-技能记忆机制.md](03-技能记忆机制.md)：10 条决策（含理由）/ 实现 / 演化路线 V1-V3 / 架构优点 / 少量缺点。 |

### 已查清的事实（决策依据）

- **Pi 原生 skills**（源码 `pi/packages/agent/src/harness/skills.ts`）：静态加载 `SKILL.md`/根 `.md`，frontmatter `name`/`description`/`disable-model-invocation`，name 须匹配父目录名。**无** Top-K 路由、trust、使用统计、自动生成。
- **现有 evopi-trace 的 memory MVP**：`.pi/evopi/memory/` 下 INDEX/MEMORY/SKILLS；`/evopi-memory add` 写 MEMORY.md + `evopi.memory` 条目 + `memory.write` 事件。
- **4 个开源参考**：claude-mem（钩子骨架+渐进披露）、mempalace（本地零 API 检索 R@5 96.6%）、memU（可读 Markdown 树+类型化抽取）、darwin-skill（棘轮自进化+多评估器）。详见调研文档 `docs/Pi开源参考项目调研_2026-06-30.md`。

---

## 当前模块详情：执行治理机制（模块 4）

**定位**：把用户请求变成「受治理的工程 job」——协调 plan / act / review / 审批 / checkpoint / 验收，而不是让工具调用作为互不相关的事件散落执行。这是 EvoPi 第一个会**主动介入执行**（拦截/审批）的模块。

### 子议题清单

| # | 子议题 | 状态 | 结论摘要 |
| --- | --- | --- | --- |
| 4.0 | 模块定位与 V1 范围 | 🟡 方案已定 | **介入强度=主动拦截**：Policy Gate 在 `tool_call` 里对高危操作真 block + 要审批（这是模块灵魂，已查证 Pi 原生支持 block）。**V1 范围**：①Job 信封（已实现，完善）②Policy Gate（核心，新建）③审批（ctx.ui.confirm）④checkpoint（Pi 原生 fork/navigate，纳入 V1）⑤验收轻量版。**自动 rewind**（检测失败自动回退）要 EvoPi 自己做，列 V1 进阶/V2。 |
| 4.1 | Job 信封模型（状态机/与 trace 绑定） | 🟡 方案已定 | 现有 `/evopi-job` 信封**不大改**，只补 3 点：①与 Policy Gate 联动（拦截/审批时更新 job 状态）②`turn_end` 把 session entryId 记进 job（供 rewind）③plan/act/review 用现有字段表达（plan=plan 字段、act=toolCalls/toolErrors、review=acceptance），**不加 phase 状态机**。**Job 与 Trace 一对多**：手动 `/evopi-job start`，一个 job 跨多轮 trace（把完整任务包成 job，不产生碎屑 job）。理由：现有信封已能 resume/绑 traceId/记成败，重设计是浪费；Execution 价值在 Policy Gate 不在信封。 |
| 4.2 | Policy Gate：风险分级与拦截 | 🟡 方案已定 | **三级**：low（只读 read/grep/find/ls、git status/log）直接放行；medium（write/edit、mutating bash）放行+记 trace；high（黑名单 rm -rf/force-push/publish/curl\|sh/secrets、受保护路径写）🛑 **block+审批**。判定=按工具类型 + 内容扫描（Bash/写入路径）。**总是拦高危，与 `ctx.isProjectTrusted()` 无关**（项目信任只决定能否启动，≠可以 rm -rf）。block 转成错误结果给模型，reason 写明「已被 EvoPi Policy 拦截+原因」。**危险动作黑名单+受保护路径抽成共享 policy 定义**（如 .pi/evopi/policy.json 或代码常量），模块 3 静态扫 skill、模块 4 运行时扫 tool_call 都读它（单一事实源，兑现决策 9）。 |
| 4.3 | 审批 UX（谁批、怎么批） | 🟡 方案已定 | **审批内联在门禁**：high 判定后先 `ctx.ui.confirm("EvoPi 拦截高危", "<命令> 是否放行?")` → 批准=handler 返 undefined 放行+记 `policy.approved`；拒绝=返 `{block,reason}`+记 `policy.denied`；job 状态联动 waitingApproval。**无 UI 模式（hasUI=false）= fail-safe**：弹不出审批就一律 block（reason 说明无法审批）。**审批粒度=支持「本会话都批准这个命令」**，但⚠️收紧作用域：①「记住」只对**具体命令指纹**生效（rm -rf /tmp/x 记住≠所有 rm 放行）②最危险的 blocked 级不允许「记住」，每次都问。理由：无 UI 保守 + 记住便利，但记住必须收紧否则一次误批放行整会话。 |
| 4.4 | checkpoint / rewind | 🟡 方案已定 | **checkpoint=加标签（零成本）**：Pi 每 turn 已有不可变 SessionEntry，checkpoint 只需 `ctx.setLabel(entryId, "checkpoint-before-<tool>")` + 存进 job 的 checkpoint 列表 `{entryId,label,时间,原因}`。**仅危险变更前（medium/high 写操作）自动打**（回退点都是改动前的干净点，少噪声）+ `/evopi-job checkpoint` 手动。**rewind=V1 只做手动** `/evopi-job rewind <checkpoint>`（调 `ctx.navigateTree`）；**自动 rewind 推 V2**（依赖 Eval 判定「何为该回退的失败」，误触发比不回退更糟）。checkpoint 打在危险写前、rewind 回到动手前，语义闭环。 |
| 4.5 | 验收（acceptance 基于证据） | 🟡 方案已定 | **V1=证据面板+辅助判定，人拍板**（自动裁决留 V2 靠 Eval，因「满足验收标准」是语义判断无 Eval 易误判）。`/evopi-job` 汇总 trace 证据：本 job 期间 tool_call/error 数、有无高危被拦、**测试是否通过**；用户切 `passed` 时若证据有 error/高危未解决则⚠️警告。**测试证据=启发式识别测试命令**（npm test/pytest/go test 等）+ 结合 tool_result 的 isError 判过没过。与「V1 观测、V2 自动化」节奏一致。 |
| 4.6 | 写 Trace 的事件结构 | 🟡 方案已定 | 独立 `job.*` / `policy.*` 事件（同 Cost/技能记忆，不污染 Trace core）。Job：已有 start/plan/acceptance/status + 补 `job.checkpoint`/`job.rewind`；Policy：`policy.check`（judgment）/`policy.blocked`/`policy.approved`/`policy.denied`。**套用 3.6 session anchor 判据**：写 session anchor=审批决策（blocked/approved/denied，stub 要求可追溯）+ checkpoint/rewind + job.start/终态 status；只进 JSONL=`policy.check`（逐次风险判定，量大）。**`policy.check` 只记 medium/high，low 不记**（只读无治理价值，不刷 trace）。 |
| 4.7 | 最终方案文档 | ✅ 已落文档 | 已写 [04-执行治理机制.md](04-执行治理机制.md)：含「Pi 现状→EvoPi 增量→实现前后对比」段（首个用新规则的文档，可作前三模块补写样板）+ 8 条决策 + 实现 + 演化路线 V1-V3 + 优点 + 缺点。 |

### 已查实事实（源码确认，全部可行）

- ✅ `tool_call` 返回 `{block:true, reason}` 能**真阻断**（转成错误结果返回给模型，非静默跳过）；`event.input` 可原地改（改参数后放行，不重校验）；多扩展 early-exit（第一个 block 立即返回）。
- ⚠️ Pi **无工具级审批**（只有 project_trust 全局开关）→ EvoPi 自己做；但 `ctx.ui` 有 `confirm/select/input/custom` 交互能力，审批 UX 用 `ctx.ui.confirm()`。
- ✅ session tree 原生 `ctx.fork(entryId)` / `ctx.navigateTree(targetId)` + `ctx.setLabel`；隐式 checkpoint 已有（每 turn 一条 SessionEntry）。**自动 rewind 要自己做**（检测失败→navigateTree）。
- ✅ `input` 钩子能 transform/handled 改写拦截，可把请求包成 job。
- 详见记忆 evopi-execution-pi-capabilities。

### 已知协调点

- **危险动作定义**与模块 3 决策 9（skill 静态扫描）共享：模块 4 是**运行时**拦截实际 tool_call，模块 3 是静态审查 skill 文本，两道防线应复用同一套危险动作定义。

---

## 当前模块详情：工具运行时机制（模块 5）

**定位**：治理外部工具与执行后端。V1 从「结构化错误 + 预算」起步，后续长成 MCP broker、沙箱策略、浏览器验收。是 EvoPi 关于「工具**可靠性与资源**」的模块（模块 4 管工具**安全准入**，模块 5 管工具**跑得好不好、花多少**）。

### 子议题清单

| # | 子议题 | 状态 | 结论摘要 |
| --- | --- | --- | --- |
| 5.0 | 模块定位与 V1 范围（与模块 4 分工） | 🟡 方案已定 | **模块 4=安全准入（保安：该不该放行）、模块 5=可靠性资源（运维：跑得好不好/花多少）**，职责分开但**共享 tool_call 钩子**（已查证多 handler 全部执行）。**V1 范围**：①错误归一化分类 ②工具预算（次数/延迟计数）③延迟/可靠性统计 ④受保护路径复用模块 4 共享 policy。**推后**：MCP broker、沙箱后端、浏览器验收（stub 明说首版不需远程沙箱）。**预算耗尽=软限告警（如 80%）+ 硬限 block**（返回错误结果「预算耗尽」，同模块 4 机制）。 |
| 5.1 | 工具失败归一化分类 | 🟡 方案已定 | **7 类**（timeout/auth/schema/permission/rate_limit/tool_bug/unknown）从 `tool_result.content[0].text`+isError **启发式推断**（Pi 无结构化错误码，全自建）：关键词/正则匹配（timeout→"timed out"、auth→"401/unauthorized"、permission→"403/EACCES"、schema→"invalid argument/validation"、rate_limit→"429/rate limit"、tool_bug→异常栈、unknown→兜底）。**内置关键词规则表，V1 不可配**（可配置留 V2）。**诚实局限**：启发式不可能 100% 准，但即使 70% 也比一个 bool 强。**V1 自建工具级超时**：tool_call 里给每个工具建带 timeout 的 AbortController，超时 abort + 归 timeout 类（补 Pi 缺的工具级超时，只 bash 原生有）。 |
| 5.2 | 工具预算（次数/延迟计数） | 🟡 方案已定 | **三维计量**：调用次数（现有已计）、累计延迟（自建计时）、错误次数（现有已计）。**层级**：per-turn（每 turn 重置，防单轮死循环）+ per-job（存进 job，与模块 4 联动，防整任务失控）。**粒度=分工具计数 + 全局总预算**（定位单工具失控 + 对不同工具设不同上限，如 read 宽 bash 严 + 总闸）。**硬限（block）只卡调用次数**（失控信号最明确），延迟/错误超阈值只软限告警（判定模糊，硬拦易误伤慢工具/自愈重试）。默认值示例：per-turn 单工具 50 次、per-job 单工具 200 次、软限 80%（可调）。 |
| 5.3 | 延迟/可靠性统计 | 🟡 方案已定 | **自建计时**：tool_call 记 `startTimes[toolCallId]=Date.now()`，tool_result 算 `latency=now-start` 后清理。扩展现有 toolStats：加 `errorsByClass{timeout:3,...}` + `latency{count,totalMs,maxMs}`。`/evopi-tools` 展示「bash: 45 次/3 错(2 timeout)/均 1.2s/最慢 8s」。**V1 只算简单统计 avg+max+count**（内存 O(1)），p50/p95 分位数留 V2（要样本窗口/t-digest，更重）。 |
| 5.4 | 与模块 4 挂点复用（tool_call） | 🟡 方案已定 | **顺序陷阱**：多 handler 全部执行但「最后非 undefined 结果生效」→ 若某工具既高危又超预算，block 原因会取决于注册顺序（偶然）。**V1 方案=单 handler 统一决策**：模块 4/5 同在 evopi-trace 扩展，用一个 tool_call handler 内部按优先级跑——先 policy 安全准入 → 再预算资源，**安全 > 资源**（rm -rf 比"次数超限"严重，用户应先知道是危险被拦），谁先命中 block 用谁 reason，确定不靠注册顺序。V2 拆成独立扩展后靠注册顺序约定（policy 后注册覆盖）。 |
| 5.5 | MCP / 沙箱方向（V1 边界） | 🟡 方案已定 | **V1 不做 MCP broker / 沙箱后端 / 浏览器验收**（stub 明说首版不需远程沙箱）。理由：MCP 是 Pi 零基础的独立大模块（发现/连接/transport 全自建）；真沙箱是重型工程（OpenSandbox 仅设计参考）；且 V1 的错误分类/预算/延迟**对 MCP 工具天然生效**（都走 tool_call），受保护路径已提供轻量防护。**留接口位**：①治理对所有工具统一生效不区分来源（V2 加 MCP 自动享受）②V1 的超时 AbortController 已探出「工具执行包装层」，沙箱是往此点加。**事件预留通用字段（toolName/errorClass/latency/source），不预先建 mcp.*/sandbox.* 专用事件**（用到再建，满足 stub「将来不改词表」验收）。 |
| 5.6 | 写 Trace 的事件结构 | 🟡 方案已定 | **扩展 `tool.result` 加 errorClass/latencyMs 字段**（客观执行属性，任何观测者都想知道多久/错哪类，是 Trace 增强非污染）+ 独立 `tool.budget` 事件（预算软限告警/硬限触发，模块 5 治理语义）。**新判据（补充"独立事件"原则）**：客观执行属性可加进 Trace core 事件，消费者语义视角才独立成事件。**session anchor**：只进 JSONL=tool.result（逐次观测量大）；写 session anchor=tool.budget 硬限 block（因预算拦截执行的治理决策，同 policy.blocked 级）。 |
| 5.7 | 最终方案文档 | ✅ 已落文档 | 已写 [05-工具运行时机制.md](05-工具运行时机制.md)：含「Pi 现状→增量→实现前后对比」段 + 8 条决策 + 实现 + 演化路线 V1-V3 + 优点 + 缺点。 |

### 待查证事实（已派 agent）

- Pi 工具错误处理有无分类基础（还是只有 isError）？工具超时机制？
- tool_result 事件能否直接拿 latency？还是要自己 tool_call 记开始时间？
- Pi MCP 支持现状？registerTool 能力？有无预算/次数限制机制？
- 多个 tool_call handler 是否都执行（决定模块 4 policy 与模块 5 预算能否共用挂点）？
- 工具在哪执行（沙箱/本机）？setActiveTools 能否动态禁用工具（用于预算耗尽降级）？

### 已知协调点

- **受保护路径/危险动作定义**：与模块 3/4 共享同一份 policy 定义（模块 4 决策 4 已抽出）。模块 5 不重造。

---

## 当前模块详情：评测协作机制（模块 6，最后一个核心）

**定位**：加子代理协作 + 可测量的回归门禁。是核心模型里「Eval = 对 Trace 的评分」的落地，也是**多处 V2 的共同依赖**（模块 3 自进化棘轮、模块 4 自动验收/自动 rewind 都要它）。做完 EvoPi 模块规划闭环。

### 子议题清单

| # | 子议题 | 状态 | 结论摘要 |
| --- | --- | --- | --- |
| 6.0 | 模块定位与 V1 范围 | 🟡 方案已定 | **V1=评分引擎 + 子代理 + 棘轮半自动**。评分引擎（确定性优先）+ golden task + 子代理（**查证发现 Pi 子代理现成，可真做 spawn 评审子代理，不必退到单 agent 自评**）；棘轮定框架但半自动（手动 replay + 分数对比 + 人审），全自动棘轮留 V2。**评分方式=确定性优先**（退出码/测试过否/文件变更/输出标记，可重复零成本不幻觉），LLM 评分作可选补充。与「V1 基础/V2 自动化」一致——Eval V1 提供评分能力让 V2 各模块自动化有裁决依据。 |
| 6.1 | 子代理委派机制 | 🟡 方案已定 | **参考 Pi subagent，spawn pi 子进程**（`pi --mode json --tools ... --model ...`）：进程级隔离最彻底（评估子代理跑测试/改文件不污染主 agent），parallel 多评估器现成；参考 examples/extensions/subagent 实现但不 vendor 外部代码（避免跟其变更）。**多评估器架构支持，V1 默认单评估器**：用 parallel 模式留 darwin-skill 式「spawn N 个评审投票」能力，但 V1 默认单个（省 token），需高置信度时（如棘轮门禁）才开多个。 |
| 6.2 | Agent card（工具/模型/预算/输出） | 🟡 方案已定 | **复用 Pi agent card frontmatter + 扩展字段**（同模块 3 决策 3 哲学）：name/description/tools/model/systemPrompt 用 Pi 原生，EvoPi 加 `maxTurns`（turn 预算，Pi 无）+ `outputSchema`（期望输出，Pi 无 responseFormat）塞额外键，存 `.pi/evopi/agents/`。**turn 预算执行**：主进程监听子进程 JSON 事件流的 message_end 计数，超 maxTurns 就 kill。**输出约束**：prompt 指导 + 手工解析 JSON。**解析失败=记为无效评分，不猜**（确定性评分不能建在猜测上，守 Eval 可信度底线，可选重试一次）。 |
| 6.3 | Golden task 数据集 | 🟡 方案已定 | **YAML fixture** 存 `.pi/evopi/evals/tasks/`，结构 `{id, description, prompt, checks[], scoring}`。check 都是**确定性**：command（跑命令看退出码）/file_changed（文件必改）/output_contains（含标记）/no_forbidden（不碰禁区）。**评分粒度两者都支持，默认 all_checks_pass**（全过=pass 二值，回归门禁够用），weighted（每 check 权重算 0-1）留细粒度场景。**V1 支持从失败 trace 生成 task 候选**（模块 4 失败 job/被拦高危→一键生成 golden task 负例，人审后入库）——兑现「从 Trace 沉淀资产」，与 stub「失败 trace 可变 test」一致，形成闭环。 |
| 6.4 | 评分：确定性检查优先 | 🟡 方案已定 | **确定性检查主力**：command（退出码）/测试/文件变更（git diff via bash 现成）/输出标记/禁区（复用模块 4/5 policy）。**退出码=子代理里显式 `命令; echo EXIT:$?` 解析**（不依赖 Pi 把退出码藏异常，可靠跨平台）。**LLM 评分只留接口默认不开**（补语义质量如可读性，V2/需要时开，省钱不引不确定性）。**评分结果绑 traceId + taskId + datasetId**（可追溯，回归门禁证据基础）：`{evalId,taskId,traceId,datasetId,score,passed,checkResults[],timestamp}`。 |
| 6.5 | 棘轮回归门禁 | 🟡 方案已定 | **棘轮=只进不退**：候选只有 replay 后分数不低于基线才启用，否则回滚（防模型自作主张的改动悄悄变差）。**V1 半自动**：候选（来自模块 3/4）→安全扫描（复用 policy）→`/evopi-eval replay <candidate>` **手动触发**→展示 baseline vs candidate→**人拍板 enable**（全自动 enable 留 V2，「agent 改自己」最危险，先人在环，同模块 3/darwin-skill）→记回滚元数据。**门禁=不降即可（持平也过）但需人审**（对齐 stub「改善或保持」，新能力可能不提分但有价值）。**replay 全量跑 golden task**（防「改 A 弄坏 B」，回归本意）。基线=候选要替换的现有资产分/历史最佳。 |
| 6.6 | 写 Trace 的事件结构 | 🟡 方案已定 | 独立 `eval.*` 事件（不污染 Trace core）：`eval.score`（已有）/`eval.run`/`eval.replay`/`eval.gate`/`eval.candidate`。**套用 3.6 判据**：写 session anchor=`eval.gate`（门禁 enable/reject 候选，同 skill.approved 级）+ `eval.candidate`（产生新 golden task，资产产生）；只进 JSONL=eval.score/run/replay（逐次评测观测量大）。**闭环点**：`eval.gate` enable 就是资产被接受时刻，可触发 `skill.accepted`/记忆生效——模块 3（技能候选）与模块 6（评分裁决）在此接上，兑现接力上下文最早提的 `evopi.skill.accepted`。 |
| 6.7 | 最终方案文档 | ✅ 已落文档 | 已写 [06-评测协作机制.md](06-评测协作机制.md)：含「Pi 现状→增量→实现前后对比」段 + 8 条决策 + 实现 + 演化路线 V1-V3 + 优点 + 缺点。 |

### 待查证事实（已派 agent）

- Pi 有无 subagent 机制 / 「subagent 示例」怎么实现？AgentHarness 能否独立实例化跑隔离子代理？
- 子代理工具集/模型/预算能否隔离（agent card 基础）？
- Pi 有无结构化输出（outputSchema）机制？还是要自己解析文本？
- 确定性评分原料：exit code 在 tool_result 哪？文件变更能否检测？worktree 隔离有无支持？

### 已知协调点

- **棘轮候选流**与模块 3 决策 8（skill trust/候选）+ 模块 4（验收）一致：候选→扫描→replay→人审→回滚。Eval 是这套「自动化裁决」的评分引擎，V2 各模块共用。
- **确定性评分**复用模块 5 的工具证据（exit code/测试识别）+ 模块 4 的证据面板思路。

---

## 当前模块详情：文档产品化机制（模块 7，收尾）

**定位**：把整个 EvoPi「产品化」——讲清楚、能演示、可度量、能对外交付。**元模块**（管 EvoPi 自己怎么被呈现，不涉及 Pi 运行时，故无「Pi 现状对比」）。**项目定位=真实可用的开源工具**（面向用户能装能用）。

### 交付物三类

- **A 文档体系**：模块文档 + 进度表 + README + 规则 —— ✅ 这几天已基本建成。
- **B Demo/演示**：每模块可跑演示 + 预期输出 —— ❌ 待代码实现后补（度量须能产出才声称）。
- **C 叙事/README**：面向用户的项目总览、安装使用、价值说明 —— ❌ V1 要补。

### 子议题清单

| # | 子议题 | 状态 | 结论摘要 |
| --- | --- | --- | --- |
| 7.0 | 定位与 V1 交付范围 | 🟡 方案已定 | 定位=真实可用开源工具；V1 交付=**文档体系收口 + 项目总览叙事**；demo/度量待实现后补 |
| 7.1 | 文档体系收口（入口/导航/一致性） | ✅ 已完成 | **模块 1 正文全中文化**（6 模块文档语言统一）。**README 改成面向用户的项目 README**（与 7.2 合流）。6 模块文档结构已统一（一句话定位→Pi 现状→决策→实现→演化→优缺点）。 |
| 7.2 | 项目总览叙事（面向用户/README） | ✅ 已完成 | **README 重写为面向用户的项目主页**：EvoPi 是什么 / 解决什么问题（痛点↔答案表）/ 核心模型 / 6 模块一览 / 与 Pi 关系 / 演进节奏 / 快速开始 + 命令表 / 文档地图 / 文档规则。 |
| 7.3 | Demo 与度量规范（先立规范，实现后填） | 🟡 方案已定 | **demo 模板**：setup（扩展路径/命令）→ 短任务 → 预期 trace 事件/输出 → 成功度量 → 已知限制。**核心约束**：度量必须 trace/测试能产出才声称（如 Trace「生成 JSONL」、Cost「命中率可算」、Execution「policy.blocked 计数」）。demo 内容待代码实现后填。写进方案文档。 |
| 7.4 | 持续维护机制 | 🟡 方案已定 | 文档与实现同步（实现后回填 demo/度量、校准文档与行为）；进度表是单一事实源持续维护；外部参考只引不抄（README 已声明）；行为级图优于文件清单。写进方案文档。 |
| 7.5 | 最终方案文档 | ✅ 已落文档 | 已写 [07-文档产品化机制.md](07-文档产品化机制.md)：交付物盘点 + 4 条决策 + demo 模板/度量规范/维护机制 + 演化路线 + 优缺点。 |

### 与其它模块关系

- 模块 7 消费所有模块的文档 + Trace 度量，产出对外叙事。它是「文档规则」的宿主（README 规则 1/2/3 都在此）。

---

## 已锁定的全局决策（跨模块约束）

### 核心模型

- **Session** = 用户与 agent 的主线语义记录（只记重要语义事件）
- **Session Tree** = Session 的 resume / fork / branch 结构
- **Trace** = agent 执行过程的完整观测证据链（记所有执行细节）
- **Eval** = 对 Trace 的评分
- **Memory / Skill** = 从高分/低分 Trace + Eval 沉淀的长期资产

### Trace 关键决策

- 不全量双写 trace 到 session；也不完全断开 session 与 trace。
- 采用 **Anchor-only session writes**：session 只写 traceId / path / summary / checkpoint 等关键锚点。
- 完整 Trace 进 **JSONL**（SQLite 列为后续规划，尚未实现）。
- Trace 是底座；Cost / Memory / Job / Tools / Eval 是上层消费者。

### 工程约定

- 工作目录 `D:\evopi`（内层 `pi/` 是只读参考仓库，已 gitignore）。
- commit message 用中文，小步提交。
- 规划阶段默认只讨论不写代码（除非明确要求实现）。
- 回复与文档尽量用中文。
- **每个决策都要写明「为什么这么选」的理由**（规则见 [README.md](README.md)）。

---

## 待办指针

**🎉🎉 全部 7 个模块（0-7）讨论完毕，最终方案文档全部落地。EvoPi V1 规划彻底闭环。**

**已完成的收尾**：
1. ✅ 前三模块补「Pi 现状对比」段（6 模块文档结构统一）。
2. ✅ 模块 1 正文中文化（6 模块语言统一）。
3. ✅ README 重写为面向用户的项目主页。
4. ✅ 模块 7 文档产品化方案落地。
5. ✅ **[00 整体架构](00-整体架构.md)**：分层图/运行时序图/事件总线全景/存储布局/协作关系/依赖顺序/演进（mermaid）。

**下一步（用户定）**：
- **进入实现阶段**：V1 规划完整，可按模块写代码到 `.pi/extensions/`（Trace/Cost 已有 MVP 可深化，或按依赖顺序）。
- **模块 7 V2**：代码实现后回填每模块可跑 demo + 真实度量。
- **git 提交**：本轮新改动（模块 7 方案、README 重写、模块 1 中文化、前三对比段）待提交。
