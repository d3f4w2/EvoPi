---
name: agent-project-refactor-planner
description: Use when refactoring or extending an existing open-source AI-agent project (coding agents, agent harnesses, agent runtimes) according to your own design ideas, and you need to discuss module-by-module and produce refactor/design documents before writing code. Covers scoping the base project, investigating what the base already implements before designing, deciding one module at a time with recorded rationale, and writing final "decision / implementation / evolution roadmap / pros / cons" docs. Continuously reused and updated across sessions.
---

# Agent 项目改造规划

## 适用范围

给一个**已存在的开源 agent 领域项目**（编码 agent、agent harness、agent runtime、记忆/工具/评测框架等）按自己的想法做改造，且当前处于**只讨论/写文档、暂不写代码**的规划阶段时使用。

产出是「改造方案文档」，不是代码。目标：把「在别人的底座上加自己的治理/能力层」这件事，讨论清楚、决策留痕、能对齐下一个人（或下一个 AI）。

不适用：从零起项目、纯 bug 修复、已进入编码实现阶段（那时改用具体实现流程）。

## 核心原则（为什么这套方法有效）

1. **先摸底座，再设计**——agent 项目的价值在于「补底座缺的那部分」，不是重造轮子。设计任何模块前，先查清「底座已经实现了什么、源码层面怎么做的」。很多设想的功能底座早已做掉（例：某编码 agent 已自动做 prompt 缓存、已做 skill 渐进披露）。
2. **查证不猜**——涉及底座能否做某事（能否拦截工具、能否改 payload、能否回退）时，**读源码确认**，不靠记忆或钩子名猜。一次「以为能在 A 钩子做、实则不行」的返工，比先查证贵得多。
3. **逐模块、逐子议题讨论**——一次只推进一个模块的一个子议题；每个子议题给用户 2-3 个带取舍的选项让其拍板，不替用户做架构决策。
4. **每个决策写明理由**——不只写「做什么」，写「为什么这样而不是那样」（含被否方案、依据的事实）。
5. **给演化路线**——每个模块写 V1（现在做什么）+ V2/V3（将来加什么、为什么推后）。避免 V1 塞太多，也让读者看到全貌。
6. **单一进度事实源**——维护一份进度表（模块总览 + 每模块子议题勾选 + 当前指针），每次对齐先看它。跨会话/跨 AI 靠它续上。
7. **保守默认 + 先观测后自动化**——治理类模块（拦截/审批/评分）默认取保守姿态；能力先做「观测/采集」，把「自动决策」留给后续版本。
8. **文档是分层交付物**——每模块方案文档（细节）+ 一份整体架构文档（全局图）+ 一份面向用户 README（门面）。三层缺一，读者就得自己拼装。整体架构文档在所有模块决策定完后才写（否则过时）。
9. **规则会中途长出来，要及时固化**——用户会在讨论中提新的文档/工作规则（如「决策写理由」「给演化路线」「讲清底座现状」）。每出现一条就写进项目的文档规则宿主（如 README），并回头让已写的文档补齐，保持结构一致。

## 工作流

### 第 0 步：建立事实基础

- 确认工作目录、仓库、底座项目位置（底座常作为只读参考子目录，勿改）。
- 找到并读：项目的模块索引/backlog、已有的设计 stub、以及**开源参考项目调研**（若有）。
- 校准现实与旧上下文的差异——目录可能被改名、某功能可能已实现非骨架、路径可能变。**以磁盘真实代码为准**，不以接力文档为准。差异要当面跟用户确认，别默默假设。

### 第 1 步：锁定全局模型与模块栈

- 和用户确认核心概念模型、模块清单、实现顺序、跨模块约束（如数据写哪、什么算关键事件）。
- 这些是所有模块的硬约束，先钉死再逐个展开。

### 第 2 步：逐模块讨论（每模块循环）

对每个模块：

1. **摸底座**：通常派一个只读探索 agent 查源码，问准「这个能力底座有没有、怎么实现的、扩展挂点在哪、能不能 mutate/block/回退」。把结论存进记忆（下个模块可能复用）。
2. **划 V1 范围**：一个模块常横跨多件复杂度递增的事（如 记忆/路由/自进化）。先和用户定 V1 做哪些、哪些推后，理由讲清。
3. **逐子议题拍板**：列子议题清单，一项项走。每项用 2-3 个带取舍选项问用户（可用带 preview 的选择）。定一个，归档一个到进度表。
4. **落最终方案文档**：全部子议题定完后写，结构见下。

### 第 3 步：写最终方案文档（每模块一份）

固定结构（可按项目微调）：

- **一句话定位** + V1 灵魂
- **底座现状 → 本项目增量 → 实现前后区别**：① 底座已实现什么、源码怎么做的（带文件行号）；② 本模块架构；③ 我们还要实现什么、如何实现；④ 实现前后行为/能力有何区别（对比表最直观）。
- **决策（含理由）**：每条「选择 + 为什么这样而不那样」。
- **实现**：钩子/挂点表、关键流程、落点。
- **演化路线**：V1 / V2 / V3 表。
- **架构优点**（几条）
- **少量缺点**（诚实列，含协调点/技术债）

### 第 4 步：维护进度表与记忆

- 进度表：模块总览表（状态图例 ✅🟡🔄⬜ + 👉当前指针）+ 每模块子议题清单 + 已查证事实 + 全局决策。
- 记忆：项目上下文、底座能力查证结果、每模块决策摘要、文档规则。跨会话靠记忆 + 进度表续上。
- 收尾时把「下一步从哪继续 + 待答问题」写进进度表指针，方便下次直接接。

### 第 5 步：写整体架构文档（所有模块讨论完之后）——别漏这一步

各模块方案文档只是「一个个房间的设计图」，读者还缺「整栋楼的平面图」。所有模块决策定完后，写一份 `00-整体架构.md`（编号 00 让它排在最前作总览入口）。它把散在各模块文档里的**整体视角**收拢到一处，包含：

- **分层架构图**：底座层 / 核心底座模块 / 消费者模块 / 元层。
- **一次运行的时序图**：各模块的钩子在同一条时间线上何时介入（哪个在请求前、哪个夹在 provider 前后、哪个共用同一挂点、哪个是离线触发）。
- **事件/数据总线全景**：所有模块的事件汇成一张表，标明各自的存储去向（主线 vs 日志）。
- **存储布局**：项目数据目录的完整树。
- **模块协作关系图**：谁依赖谁、共享什么（共享定义/共用挂点/裁决引擎）。
- **实现依赖顺序图**：先做谁后做谁、谁解锁谁。
- **整体 V1→V2→V3 演进**：把各模块的演化路线汇成三阶段主题。

图用 mermaid（GitHub/编辑器可渲染）。**时机很重要**：图只在所有行为决策定完后画，否则会过时（这也常是底座 stub 自己的要求）。这一步是「文档产品化/收尾」的核心，它把 N 篇模块文档串成一个任何人都能快速建立全局认知的整体。

### 第 6 步：产品化收口（可选，取决于项目定位）

先问清项目定位（开源工具 / 简历作品 / 学习记录），它决定收口重心：

- **开源工具** → README 改成面向用户的项目主页（是什么/解决什么/核心模型/模块一览/快速开始/文档地图）；每模块 demo。
- 统一文档：语言一致（早期 stub 常是英文，其余中文）、结构一致（每篇同样的段落骨架）。
- **诚实原则**：代码没实现前，不写「提升 X%」这类测不出的度量——把「讲清楚」和「可演示/度量」分开，度量待有真实产出（trace/测试能产出）才声称。

## 关键手法

- **派探索 agent 查源码**：问题要具体到「返回什么值能 block」「event.input 能不能改」「哪个钩子能过滤 X」，并要求带文件行号引用、找不到就明说不猜。
- **决策问题给可对比选项**：每个 AskUserQuestion 的选项写清取舍（推荐项标注 + 一句话说清代价），必要时附 preview 做可视对比。
- **归档即时化**：每定一个子议题，立刻更新进度表对应行 + 指针，别攒着。
- **跨模块判据复用**：某模块确立的判断标准（如「什么事件写主线、什么只进日志」），后续模块直接套用并在进度表标注「可复用」。
- **早识别模块间协调点**：两个模块碰同一挂点/同一定义时（如都在 tool_call 拦截、都要危险动作清单），提前定「谁负责什么、共享哪份定义」，别让两套定义漂移。
- **多 handler 共用一个钩子要防顺序陷阱**：若底座「多 handler 全执行、最后结果生效」，两个模块各返回结果会靠注册顺序决胜——用「单 handler 内部按优先级统一决策」消除偶然性（如 安全 > 资源）。
- **客观属性 vs 消费者视角**：往底座 core 事件加字段要区分——客观执行属性（耗时/错误类）任何观测者都想要，可加进 core 事件；某模块的语义视角（成本/评分）才独立成事件，避免污染 core。

## Update Rule

每当发现新的坑、更好的做法、或用户提出新的文档/工作规则时，**在同一会话内更新本 skill**，并单独 commit 这次更新。

## 本项目（EvoPi）实战教训

- 底座（Pi 编码 agent）**已自动做 Anthropic prompt 缓存**（占了 4 个 cache_control 断点里的 3 个）——手动再加是重复且有害。→ 先查底座缓存机制再设计 Cost 模块。
- 原以为能在 `context` 钩子做 skill 的 Top-K 路由，**查源码发现不行**（系统提示在该钩子前已固定）——正确挂点是 `resources_discover`。查证救回了一次错误设计。
- 底座的 `tool_call` 钩子**能真 block**（返回 `{block,reason}` 转成错误结果给模型）、`event.input` 可原地改、`ctx.ui` 有 confirm/select/input——执行治理模块技术上全可行，查实后才敢定「主动拦截」。
- 用户会中途重组目录（`docs/evopi/` → `docs/evopi-v1/`）或留「小记」提新要求——**每次开工先核对磁盘真实结构**，把新规则并入文档规则。
- 「项目受信任」≠「可以执行危险操作」——治理模块要区分这类语义，别混用底座的信任开关。
- 保守默认 + 先观测后自动化：Cost 先观测缓存命中率（V2 才优化）、记忆自动抽取先做候选（人审后生效）、验收先摆证据（V2 才自动裁决）、rewind 先手动（V2 才自动）。这个节奏反复出现，是可靠的 V1 收敛法。
- 底座的子代理能力可能比预期强：Pi 有完整 subagent（进程隔离/工具+模型隔离/agent card frontmatter 格式）——摸底后「评测子代理」不必自建，复用即可。**摸底前别假设底座弱**。
- 底座某能力「几乎全空白」也是重要结论（如 Pi 无错误分类/预算/延迟/MCP/沙箱）——意味着增量价值大但全要自建，且能大胆设计不怕和底座冲突。
- 共享定义避免漂移：危险动作黑名单/受保护路径被模块 3（静态扫）、4（运行时拦）、5（工具禁区）三处用 → 抽成一份共享 `policy.json`，别各搞一套。
- 让模块间形成闭环：失败的执行可沉淀成评测用例（负例）；评测门禁通过 = 资产被接受的时刻（`eval.gate` enable 触发 `skill.accepted`）——这些闭环让「从执行沉淀长期资产」落地，是加分的架构叙事。
- 有些「元层」模块（文档/评测的框架）依赖别的模块的产出，其完整能力天然是后续版本——V1 先立框架/规范，别硬做。
